https://node.windy.com/obs/hurricanes/history

https://www.djamware.com/post/5c6ccd1f80aca754f7a9d1ec/push-notification-using-ionic-4-and-firebase-cloud-messaging 


https://mapsplugin.github.io/ionic-googlemaps-quickdemo-v4/polygon --- map demo


https://github.com/DIEUDONNE-DENGUN/DenPlaces/blob/master/src/pages/hospital/hospital.ts

https://www.joshmorony.com/create-a-nearby-places-list-with-google-maps-in-ionic-2-part-1/

https://codepen.io/jasonmayes/pen/DupCH

//algo for checking if currnet loc inside polygon
pointInPolygon(polygonPath, coordinates) {
        let numberOfVertexs = polygonPath.length - 1;
        let inPoly = false;
        let { lat, lng } = coordinates;

        let lastVertex = polygonPath[numberOfVertexs];
        let vertex1, vertex2;

        let x = lat, y = lng;

        let inside = false;
        for (var i = 0, j = polygonPath.length - 1; i < polygonPath.length; j = i++) {
            let xi = polygonPath[i].lat, yi = polygonPath[i].lng;
            let xj = polygonPath[j].lat, yj = polygonPath[j].lng;

            let intersect = ((yi > y) != (yj > y))
                && (x < (xj - xi) * (y - yi) / (yj - yi) + xi);
            if (intersect) inside = !inside;
        }

        return inside;
}            





// ML API

https://eu-gb.functions.cloud.ibm.com/api/v1/web/Ankita.Ghosh%40cognizant.com_dev/default/GetFloodPredictionV1.json
